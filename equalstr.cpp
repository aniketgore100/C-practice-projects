#include <bits/stdc++.h>
using namespace std;

int main(){
    // a b a c a b a
    // b a c a b a a
    string str1, str2;
    cin>>str1>>str2;
    int n = str1.length(), m = str2.length();
    j = 0;
    for(int i=0; i<n; i++){

    }
}
